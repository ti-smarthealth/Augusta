{
  "name": "tish-backend-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "App-facing API Lambda. Auto-deployed by .github/workflows/deploy-backend.yml on push to main; see DEPLOY.md for the one-time IAM setup.",
  "dependencies": {
    "pg": "^8.16.0"
  },
  "scripts": {
    "test": "node --test",
    "migrate": "node migrate.mjs up",
    "migrate:status": "node migrate.mjs status",
    "migrate:dry-run": "node migrate.mjs up --dry-run"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.1100.0",
    "@aws-sdk/client-s3": "^3.1137.0",
    "@aws-sdk/s3-request-presigner": "^3.1137.0"
  }
}
